[build]
  command = "npm run build"
  publish = "dist"
  environment = { NODE_VERSION = "20", SECRETS_SCAN_OMIT_KEYS = "BIGQUERY_PROJECT_ID,BIGQUERY_DATASET,GOOGLE_CLOUD_CREDENTIALS_BASE64" }

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[headers]]
  for = "/api/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Headers = "Content-Type, Authorization"
    Access-Control-Allow-Methods = "GET, POST, PUT, DELETE, OPTIONS"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  port = 8888
  functionsPort = 9999